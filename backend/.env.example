# Server Configuration
NODE_ENV=production
PORT=3001
SERVER_HOST=0.0.0.0

# CORS Configuration
# For production, set this to your frontend domain(s)
# For development, you can set to 'true' to allow all origins
# For multiple domains, use comma-separated values: https://domain1.com,https://domain2.com
CORS_ORIGIN=https://yourdomain.com

# MediaSoup Configuration
# The IP address that will be announced to clients for WebRTC connections
# This should be your server's public IP address or domain
MEDIASOUP_ANNOUNCED_IP=0.0.0.0

# Port range for WebRTC media streams
# Make sure these ports are open in your firewall
MEDIASOUP_MIN_PORT=40000
MEDIASOUP_MAX_PORT=49999

# Optional: MediaSoup Worker Configuration
# Number of MediaSoup workers (default: 1)
# MEDIASOUP_NUM_WORKERS=1

# Optional: Logging Configuration
# LOG_LEVEL=info

# Optional: SSL/TLS Configuration (if using HTTPS)
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# Optional: Database Configuration (if you add database support later)
# DATABASE_URL=postgresql://user:password@localhost:5432/webrtc_db
# REDIS_URL=redis://localhost:6379
